# Production overrides for deployed docs site (Vercel / container)
# Copy to .github/env.production.local (gitignored) before sync scripts

AI_PROVIDER=openai
MEMORY_PROVIDER=none
VECTOR_PROVIDER=none
EMBEDDING_PROVIDER=openai
MCP_ENABLED=false
KORTEX_DEBUG=false
LOG_LEVEL=info
